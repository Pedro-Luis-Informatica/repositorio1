# 🚀 Desafío de JavaScript

---

| 🟢 Fácil | 🟡 Intermedio | 🔴 Difícil |
| :--- | :--- | :--- |
| **1.** Pide el nombre al usuario con `prompt` y salúdalo con `alert`. | **2.** Pide un número del 1 al 7 y muestra el día de la semana con `switch`. | **3.** **Login:** Si el usuario es "admin", pide clave; si es "123", da acceso. |
| **4.** Solicita un número y muestra si es positivo o negativo usando `if`. | **5.** Determina si un número es positivo, negativo o cero usando `else if`. | **6.** **Cajero:** Pide un PIN; si es correcto, muestra menú Retirar/Depositar. |
| **7.** Pregunta la edad; si es ≥ 18, muestra "Acceso permitido". | **8.** Pide una hora (0-23) y saluda: "Buenos días", "Tardes" o "Noches". | **9.** **Préstamo:** Si es mayor de 21 y gana > 2000, muestra "Aprobado". |
| **10.** Pide un número y determina si es par o impar con el residuo (%). | **11.** Crea un sistema de notas: 90+(A), 80(B), 70(C), <70(F). | **12.** **Puerta digital:** Requiere nombre en "autorizados" AND código correcto. |
| **13.** Usa el **operador ternario** para ver si un número es > 100. | **14.** Menú de bebidas (1. Café, 2. Té) y usa `switch` para mostrar precios. | **15.** **Estudiante:** Si promedio > 70 y asistencia > 80%, aprobado (anidación). |
| **16.** Solicita dos números y muestra con un `alert` cuál es el mayor. | **17.** Solicita un mes y responde a qué estación del año pertenece. | **18.** **Inventario:** Antes de agregar un dato, usa `.includes()` para no repetir. |
| **19.** Pide una contraseña y verifica si coincide con "Secreto123". | **20.** Calculadora básica (+, -, *, /) usando dos números y `switch`. | **21.** **Filtro:** Si un array de comentarios incluye "spam", muestra advertencia. |
| **22.** Pide temperatura; si es > 30°C, muestra "Clima caluroso". | **23.** Escribe un algoritmo que determine si un año es bisiesto. | **24.** **Validación:** Verifica que un nombre no esté vacío y tenga > 3 letras. |
| **25.** Pregunta si tiene hambre ("sí"/"no") y da un consejo diferente. | **26.** Pide tres lados y determina si el triángulo es equilátero o escaleno. | **27.** **Cine:** Precio 10; si tiene cupón (verificar en array), baja a 7. |
| **28.** Solicita un número y determina si es múltiplo de 5. | **29.** Define un `array` con 5 frutas y muéstralo en la consola. | **30.** **Final:** Pide nombre, valida si está en un array y saluda. |
| **31.** Crea un array de colores y muestra el tercer elemento (índice 2). | **32.** Usa `.includes()` para ver si "Verde" existe en un array. | **33.** Si la longitud de un array de tareas es > 5, muestra "Agenda llena". |
| **34.** Declara un array de estudiantes y muestra cuántos hay con `.length`. | **35.** Pide un nombre y verifica si está en un array de "Invitados VIP". | **36.** Verifica si un usuario tiene el rol "Editor" en un array de permisos. |
| **37.** Crea un array de números (1-5) y cambia el primer elemento a 0. | **38.** Verifica si el número 10 está en un array y muestra éxito. | **39.** Si un array incluye "Leche", agrega "Cereal" automáticamente. |
| **40.** Agrega un elemento al final de un array de deportes. | **41.** Pide una palabra y comprueba si existe en un array "diccionario". | **42.** Verifica si un array **no** incluye "Pera" y, de ser así, agrégala. |
| **43.** Crea un array vacío y usa tres `prompt` para llenarlo con países. | **44.** Declara materias y usa `.includes()` para buscar "Programación". | **45.** Comprueba si la cantidad de elementos en un array es par o impar. |
| **46.** Muestra el último elemento de un array usando `.length - 1`. | **47.** Crea un array de lotería y verifica si el del usuario ganó. | **48.** Lista de palabras prohibidas; si el mensaje incluye una, "Bloqueado". |
| **49.** Declara un array con tres números y suma el primero y el segundo. | **50.** Pregunta una fruta; si está en la lista muéstrala, si no, "Error". | **51.** Suma dos precios de un array; si es > 50, aplica "descuento". |
| **52.** Crea un array de laptops y si tiene > 3 muestra un aviso. | **53.** Usa `switch` para evaluar notas (A, B, C) y dar un mensaje. | **54.** **Anidación:** Pide edad y si tiene licencia para poder conducir. |
| **55.** Une los elementos de un array de animales en un solo `alert`. | **56.** Pide un índice (0-4) y muestra el valor de esa posición en un array. | **57.** Determina el rango: si puntos > 1000 es "Pro", si no "Novato". |
| **58.** Usa `switch` para tallas: "S" = Pequeña, "M" = Mediana, "L" = Grande. | **59.** Pide posición y nuevo valor para reemplazar un dato en un array. | **60.** Verifica si el nombre termina con "a" usando `.length - 1`. |
| **61.** Semáforo: Pide un color y usa `switch` para (parar/seguir). | **62.** Antes de agregar a una lista, verifica duplicados con `.includes()`. | **63.** En un array de usuarios, verifica si el índice 0 es "admin". |
| **64.** Busca si el número 7 está en un array y confirma su hallazgo. | **65.** Conversor: El usuario elige moneda y se calcula el cambio con `if`. | **66.** Trivia: Si responde bien la pregunta, suma un punto a una variable. |
| **67.** Clasifica una palabra como "Corta" (< 5) o "Larga" (≥ 5). | **68.** Inventario: Si un array tiene < 2 elementos, muestra "Stock crítico". | **69.** Verifica si una lista de tareas está vacía comparando `.length` con 0. |
| **70.** Usa `switch` para categorías: "Perro" -> Mamífero, "Loro" -> Ave. | | |

---
© 2026 - Guía de Práctica para Estudiantes de Programación
